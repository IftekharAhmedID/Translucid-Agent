export const RESEARCH_PROMPT_CONTRACT = "Begin from /workspace/case/input/manifest.json, document.json, and document.txt. You are the only investigator: do not delegate, call task, or create specialist memos. First load `professional-investigation`; build a material-target queue and work in evidence waves. Load `historical-footprint` only for a remaining Tier-A historical gap after discovery finds a useful person, project, URL, or domain. Use permitted provider tools plus source.inventory/source.excerpts in this same session. Search results are leads, never citations; fetch a lead directly before using it as evidence. Preserve exact captured S references, never refetch merely to recover context, and keep source authority, timeframe, and confidence facets separate. Subject-controlled material cannot alone establish a consequential external claim unless it is the authoritative system of record. Continue only while a materially different reasonable route could change the judgment. Then call research.state.set once with every material claim, exact supporting/conflicting S references, status, importance, and remaining gap. Set publicationReady true only after the final material gap pass, then stop.";

export function researchPrompt(deadline?: string): string {
  const hostLimit = deadline
    ? `The host research-freeze deadline is ${deadline}; the total run ceiling is thirty minutes. If research completes early, leave the remaining time for publication.`
    : "This is an uncapped qualification run: no host time or budget limit is enforced. Preserve usage telemetry and stop only when the inquiry is genuinely complete.";
  return `${RESEARCH_PROMPT_CONTRACT} ${hostLimit} State uncertainty honestly and do not manufacture corroboration.`;
}
