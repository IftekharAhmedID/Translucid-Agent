export const RESEARCH_PROMPT_CONTRACT = "Begin the headless investigation from /workspace/case/input/manifest.json, document.json, and document.txt. You are the only investigator: do not delegate, call task, or create specialist memos. Start with supplied URLs and identity anchors, build a complete claim checklist, and use the permitted provider tools plus source.inventory/source.excerpts in this same session. Search broadly, fetch promising results or direct leads with an optional claim focus, and preserve every useful captured S reference. Search results are leads, not citations. For consequential claims, stop at a dispositive authoritative primary record, authoritative evidence plus materially independent corroboration, or exhausted materially different public routes. Subject-controlled or subject-authored material may establish a lead but cannot alone establish a consequential claim unless it is itself the authoritative system of record. Split claims whenever source authority, timeframe, or confidence differs; keep formal title/work scope, degree/UK-equivalence, and self-reported use/governance contribution as separate facets. Before declaring the ledger publication-ready, prioritize consequential unresolved claims and continue materially different reasonable public routes while they remain likely to change the judgment. Stop when dispositive evidence is captured or reasonable routes are exhausted. Do not repeat substantially equivalent searches and never refetch captured material merely to recover context. Then call research.state.set with every material claim, exact supporting/conflicting S references, status, importance, remaining gap, and publicationReady true only when final gap closure is sufficient for publication. Stop after the ledger is saved.";

export function researchPrompt(deadline?: string): string {
  const hostLimit = deadline
    ? `The host research-freeze deadline is ${deadline}; the total run ceiling is thirty minutes. If research completes early, leave the remaining time for publication.`
    : "This is an uncapped qualification run: no host time or budget limit is enforced. Preserve usage telemetry and stop only when the inquiry is genuinely complete.";
  return `${RESEARCH_PROMPT_CONTRACT} ${hostLimit} State uncertainty honestly and do not manufacture corroboration.`;
}
